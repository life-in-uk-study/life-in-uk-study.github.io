# Life in the UK 学习网站开发规范

本文件适用于本仓库全部目录。开发者和自动化 Agent 在修改代码、题库、解释、图片或文档前，必须遵守以下规则。

## 1. 项目目标

- 为中文母语学习者提供 Life in the UK Test 的双语练习和历史背景学习体验。
- 考试训练优先保证题目准确；学习模式同时帮助用户理解答案，而不是只背选项。
- 不得暗示本项目受英国政府、官方考试机构或第三方题库网站认可或运营。
- 不得宣传题库为“官方真题”、保证覆盖真实考试，或承诺通过率。

## 2. 当前题库范围与授权边界

- 当前基线为 `lifeintheuktestweb.co.uk` 的 17 套 Exam，每套 24 题，共 408 条题目记录。
- 项目所有者已确认获得使用这 17 套题目及选项的授权。保留数据中的来源网址和采集信息，以便追溯。
- 授权只覆盖项目所有者明确说明的范围。不得自行从其他付费 App、网站、书籍或题库复制内容。
- 新增第三方题目、图片、音频或长篇文字前，必须记录许可、许可证或明确授权。
- 相同题干可能在不同 Exam 中重复出现；这是源数据的一部分，不得擅自去重。

## 3. 题目完整性：最高优先级

以下字段必须忠实保留，不得改写、纠错、翻译后替换或调整顺序：

- 英文题干；
- 英文选项文字；
- 选项顺序；
- 正确答案映射；
- 单选或多选类型；
- Exam 编号及题目顺序。

如发现疑似拼写错误、事实冲突或答案错误：

1. 不直接修改源题；
2. 记录题目 ID、原文、来源页面和发现的问题；
3. 在独立勘误字段或项目文档中说明；
4. 得到项目所有者确认后，才能改变用户实际作答数据。

稳定题目 ID 使用 `exam-XX-qYY` 格式。已有 ID 不得因排序、界面重构或解释更新而改变。

## 4. 数据文件的职责

- `data/lifeintheuktestweb_exams_1_17.json`：按 Exam 分组的采集结果。
- `data/lifeintheuktestweb_questions.json`：扁平化题目数据。
- `data/enriched_exams/exam-XX.json`：按 Exam 拆分的学习增强数据；网站运行时只加载当前 Exam。
- `data/explanations_zh/exam-XX.json`：按 Exam 拆分、以稳定题目 ID 为键的中文解释源文件。
- `data/learning_overrides/exam-XX.json`：按 Exam 拆分的英文解释及题目级权威来源映射；未提供时兼容生成器中的旧版覆盖配置。
- `data/authority_sources.json`：权威来源登记表。
- `data/lifeintheuktestweb_duplicate_report.json`：重复题报告。
- `EXAM_SOURCE_AND_COVERAGE.md`：题库来源、覆盖范围和限制说明。

采集题库、重复报告和 `enriched_exams/` 属于生成产物，不要手工编辑。`authority_sources.json` 与 `explanations_zh/` 是可编辑的内容源；修改后只重新生成受影响的 Exam 并校验。

## 5. 数据生成流程

标准流程为：

```text
授权来源页面
  -> scripts/scrape_lifeintheuktestweb_exams.py
  -> 分组及扁平题库 JSON
  -> scripts/enrich_question_content.py
  -> 学习增强 JSON + 每题 SVG
```

常用命令：

```bash
python3 scripts/scrape_lifeintheuktestweb_exams.py
python3 scripts/enrich_question_content.py
python3 scripts/validate_question_data.py
python3 scripts/validate_enriched_content.py
git diff --check
```

只修改一套题时，使用 `python3 scripts/enrich_question_content.py --exam N`，仅重新生成该 Exam 的24题增强数据和备用 SVG。省略 `--exam` 才会生成全部17套。

- 抓取脚本会访问外部站点，只在授权仍然有效且确实需要刷新数据时运行。
- 刷新题库后必须审查差异，尤其是题干、选项、正确答案、题目数量和 ID。
- 不得为了让校验通过而放宽核心完整性检查。

## 6. 每道题的学习内容要求

每道题必须包含：

- 与题干及正确答案直接相关的配图；
- 清楚说明“为什么选这个”的文字解释；
- 至少一个权威信息来源；
- 可访问的图片替代文字；
- 能区分考试答案与补充历史背景的内容结构。

解释内容应遵循：

- 先直接回答为什么该选项正确，再补充历史背景或记忆线索。
- 不照搬无授权的第三方解释；新增解释应为原创表述。
- 事实、日期、人物、制度和统计数据必须能由列出的来源支持。
- `Life in the United Kingdom: A Guide for New Residents` 是考试范围的主要依据；GOV.UK、UK Parliament、ONS、The National Archives、Historic England 等可作为事实补充来源。
- 链接应尽可能指向具体、稳定的权威页面，而不是只指向机构首页。
- 若权威来源之间与题库答案不一致，不得掩盖冲突；应标注并交由项目所有者决定。
- 中文解释应自然、简洁，关键英文名称、制度名和考试关键词应保留，便于用户在英文考试中识别。
- 界面提供中英文切换；授权题干和选项始终保持英文原文，其他界面文案及解释随语言切换。

## 7. 图片和动图规范

- 当前默认素材是项目生成的原创 SVG，路径为 `public/images/questions/`；当题目能明确匹配真实素材时，以真实素材替换默认图。
- 真实人物题使用有来源和授权记录的真实照片或历史肖像；吉祥物、角色题使用实际吉祥物或角色图片；地理题使用地图；电影关联题使用真实电影海报。
- 右栏只保留一个主图片区和一个连续的文字解释区。不得另建“电影联想”板块；电影趣闻合并到答案解释之后。
- 图片必须帮助理解题干、答案或历史背景，不能只是无关装饰。
- 如果图片直接显示或强烈暗示正确答案，只能在用户提交答案后展示；遵守数据中的 `display_after_answer`。
- 图片不是事实证据。事实仍必须由权威文字来源支持。
- 每张图必须有与该题对应的 `alt` 文本，并在窄屏、放大字体和深浅色背景下保持清晰。
- 不得出现文字裁切、无法辨认的低对比度、依赖颜色区分对错，或自动播放且无法停止的动图。
- 动图应支持“减少动态效果”设置，并提供静态替代。
- 不得直接盗链第三方图片。引入外部素材时，记录原始 URL、作者、许可证、归属要求和下载日期。
- 含答案的图不可用于答题前预加载区域的无障碍文本、文件名提示或其他会泄题的位置。

## 8. 网站行为与无障碍

- 学习模式与模拟考试模式必须明确区分。
- 模拟考试采用 24 题、45 分钟、答对 18 题通过的规则时，应在界面中注明规则来源和更新时间。
- 多选题必须明确提示需要选择的答案数量（若源数据可确定），评分必须比较完整答案集合。
- 键盘用户必须能完成选项选择、提交、下一题、查看解释及重新练习。
- 使用语义化 HTML；表单控件必须有可见标签和清晰焦点状态。
- 正误状态不得只依靠颜色表达。
- 手机优先，并验证常见桌面宽度；题干、选项和引用链接不得横向溢出。
- 默认不采集个人数据，也不持久化答题进度或错题。若未来增加账户、分析、浏览器存储或云同步，必须先补充隐私与数据保留规范。

## 9. 实现约束

- 第一版技术基线见 `TECH_STACK.md`：React、React Router Framework Mode、TypeScript、Vite 和原生 CSS。
- 第一版采用 SPA 静态部署，不增加后端、数据库或账户系统；需求变化时先更新技术决策文档。
- 不引入不必要的大型依赖或另建重复应用。
- 应用层按当前路由读取 `/data/exams/exam-XX.json`，不得在首页或单套考试页面加载完整408题数据，也不在组件中复制题目。
- 题目数据、评分逻辑、解释内容和表现层应分离。
- 公共图片 URL 使用 `/images/questions/...`；不要把本机绝对路径写入网站数据。
- 代码标识符、数据键和测试名使用英文；用户界面可采用中英双语。
- 对不可信数据进行安全渲染，不使用未经清理的 HTML 注入。
- 保持现有用户改动；不得用破坏性 Git 命令清除无关变更。

## 10. 必须验证的内容

涉及题库、解释、来源或图片的改动，完成前至少运行：

```bash
python3 scripts/validate_question_data.py
python3 scripts/validate_enriched_content.py
git diff --check
```

验证结果必须继续满足：

- 17 套 Exam；
- 每套 24 题；
- 共 408 个稳定且唯一的题目 ID；
- 题干、选项、答案及题型无缺失；
- 每题有解释、权威来源和存在于磁盘的配图；
- 已知重复题被保留并可报告。

前端建立后，还应覆盖以下自动化测试：

- 单选和多选评分；
- 24 题组卷及 18/24 通过边界；
- 计时结束和手动交卷；
- 答题前不泄露答案，交卷后显示解释和图片；
- 刷新页面后重新开始当前考试，不恢复上一会话进度；
- 键盘操作、焦点顺序和基础无障碍检查；
- 手机与桌面关键页面的视觉回归。

## 11. 完成定义

一项改动只有在以下条件都满足时才算完成：

- 用户要求的行为已实现；
- 没有改变未经授权的题干、选项或答案；
- 新增事实有可访问的权威来源；
- 新增视觉内容匹配对应题目且不会提前泄题；
- 相关生成器、数据、素材和文档保持同步；
- 必需校验及相关测试通过；
- 已检查差异，不包含密钥、个人数据、临时文件或无关改动；
- 交付说明清楚列出改动、验证结果和任何仍存在的限制。
