# Life in the UK Web 技术选型

状态：已确定，可作为第一版网站的实现基线。

## 1. 产品边界

- 只提供网页版，重点适配 iPhone Safari、Android Chrome 和常见桌面浏览器。
- 第一版不需要应用服务器、数据库、账户系统或原生 App。
- 网站采用响应式设计，适配手机、平板和桌面浏览器。
- 题目、解释、权威来源和配图随静态站点发布；不保存个人资料、答题进度或错题，考试结果只存在于当前 SPA 内存会话中，刷新后清除。

## 2. 核心技术

| 层次 | 选择 | 用途 |
| --- | --- | --- |
| UI | React | 题目交互、当前会话答题状态和解释 |
| 应用框架 | React Router 7 Framework Mode | 页面路由、按页面拆包和未来扩展能力 |
| 渲染模式 | SPA Mode，`ssr: false` | 生成可部署到静态托管的网页，不运行服务器 |
| 构建工具 | Vite | 本地开发和生产静态资源构建 |
| 语言 | TypeScript，开启 strict | 约束题型、答案集合、学习记录和来源数据 |
| 样式 | 原生 CSS + CSS Modules + CSS Custom Properties | 保持依赖少，便于精确实现移动端和无障碍设计 |
| 本地状态 | React state + Context | 管理当前考试、选择、交卷结果和界面语言 |
| 安全 | CSP + 静态托管安全响应头 | 限制脚本、资源来源、嵌入和敏感浏览器能力 |
| 单元测试 | Vitest + React Testing Library | 验证评分、复盘、界面状态和组件行为 |
| 端到端测试 | Playwright | 验证手机视口、完整答题流程和无障碍关键路径 |
| 包管理 | npm | 依赖和脚本管理 |
| 部署 | GitHub Pages + GitHub Actions | 无服务器发布；构建时自动适配仓库子路径，并以 Hash路由避免 SPA刷新404 |

依赖安装时采用当时稳定版本并提交 lockfile；不要在代码中依赖实验性 React Server Components。

## 3. 为什么选择这一方案

- 本项目的主要复杂度来自答题交互和学习状态，React 的组件模型适合复用题卡、选项、解释、来源与统计界面。
- 页面包含首页与 17 套分套考试；React Router 负责清晰的页面地址和导航。
- React Router 的 SPA Mode 会在构建时生成静态入口，因此无需维护 Node 服务器，仍能保留类型化路由和拆包能力。
- TypeScript 能在运行前发现单选、多选、题目 ID 和答案数据结构的不匹配。
- 原生 CSS 足以完成本项目的视觉系统，暂不增加 Tailwind、CSS-in-JS 或大型组件库。
- 第一版所有内容均为公开静态学习资料，不需要后端。省去账户、数据库、服务器成本和隐私负担。

## 4. 暂不采用

- **Next.js**：第一版不需要服务端渲染、Server Actions 或 API 路由，使用它会增加部署和运行概念。
- **原生 iOS/Android 或 React Native**：用户只要求手机浏览器网页，响应式网站已经满足当前需求。
- **Supabase/Firebase**：当前没有登录、跨设备同步或多人数据需求。
- **Redux/Zustand**：第一版状态规模可以由 React 内置状态管理清楚表达。
- **Tailwind/大型 UI 库**：不需要为有限的学习页面引入额外设计抽象。
- **整包嵌入题库到 JavaScript**：会拖慢首屏解析；题库应作为静态 JSON 按需获取。

以上选择不是永久禁止。当出现明确需求时，以真实使用场景和测试结果重新评估。

## 5. 数据加载方案

- 网站读取由现有脚本生成的数据，不复制或手工维护第二份题库。
- 构建阶段把学习增强数据转换成适合网页加载的静态文件，并按 Exam 或用途拆分。
- 首页和导航不加载完整题库。
- 进入指定 Exam 时只加载该套 24 题及其必要来源。
- 每套运行时数据位于 `public/data/exams/exam-XX.json`；中文解释在生成阶段合并，浏览器无需再获取408题总表或全量翻译文件。
- 随机模拟模式可以加载轻量题目索引，再按选中题目读取内容。
- 题目图片保持独立文件，由浏览器按当前题目懒加载。

## 6. 第一版页面结构

```text
/
/exam/1
/exam/:examNumber
```

- `/`：网站介绍和 17 套考试入口。
- `/exam/:examNumber`：固定 17 套考试，练习界面不计时；首页说明正式考试为24题、45分钟、答对18题通过。

## 7. 移动端基线

- 以 360 CSS px 宽度作为最窄主要测试视口。
- 正文默认至少 16 px，触控目标至少约 44 × 44 CSS px。
- 使用 `100dvh` 处理移动浏览器动态地址栏，不依赖固定 `100vh`。
- 选项整行可点击，避免小型单选框成为唯一触控区域。
- 页面必须支持 200% 文本缩放、键盘操作和减少动态效果设置。
- 题目导航、选项和操作按钮在窄屏下不得遮住内容或浏览器安全区域。

## 8. 第一阶段目录建议

```text
app/
  components/
  features/
    exam/
    learning/
  routes/
  services/
  styles/
  types/
public/
  data/
  images/questions/
tests/
```

题目评分和会话逻辑放在 `hooks` 或 `services` 中，不写进页面组件。来源数据类型放在 `types` 中。网站不使用 `localStorage`、`sessionStorage`、Cookie 或云端服务记录答题行为。

## 9. 第一版实施顺序

1. 建立 React Router + TypeScript 项目并接入全局移动端样式。
2. 建立题库构建转换和 TypeScript 数据类型。
3. 完成一套 24 题的答题、评分、解释和配图流程。
4. 接入全部 17 套考试。
5. 完成手机视口、键盘、屏幕阅读器和性能验证后发布。

## 10. 重新评估后端的触发条件

只有出现以下需求之一时，才开始选择后端技术：

- 登录及跨设备同步；
- 付费订阅；
- 管理员在线编辑内容；
- 多用户排行榜或社交功能；
- 需要隐藏在服务器端的密钥或第三方 API；
- 浏览器本地存储无法满足的数据量或一致性要求。
